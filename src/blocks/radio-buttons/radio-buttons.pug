mixin radio-buttons(name, options)
  .radio-buttons
    each option, index in options
      .radio-buttons__item
        input.radio-buttons__input(type="radio" id="radio-buttons-"+index value=option.value name=name)
        label.radio-buttons__label(for="radio-buttons-"+index) #{option.name}