mixin radio-buttons(options)
  div(class="radio-buttons")&attributes(attributes)
    each item in options.items
      .radio-buttons__item
        input.radio-buttons__input(type="radio" id=item.id  value=item.value name=item.name)
        label.radio-buttons__label(for=item.id) #{item.label}